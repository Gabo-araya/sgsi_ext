- load i18n

if app_environment == "debug"
  .environment-badge.debug
    span.visually-hidden {% trans "Current environment" %}:

    | Debug
else
  .environment-badge.alternate
    span.visually-hidden {% trans "Current environment" %}:

    | {{ app_environment }}
