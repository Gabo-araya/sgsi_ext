# PostgreSQL settings
POSTGRES_USER=jenkins
POSTGRES_PASSWORD=jenkinspassword
POSTGRES_DB=jenkins_test

# see docker-compose.ci.yml for service definition
PGHOST=postgres
PGPORT=5432
PGUSER=jenkins
PGPASSWORD=jenkinspassword
PGDATABASE=jenkins_test
POSTGRES_HOST_AUTH_METHOD=trust

# Django settings
SECRET_KEY=itsnotsosecretbutisenoughfortestingpleasedonotcopy(c)magnet
DEBUG=True
